/**
 * LocalizationIcon — Testing › L-QA · localization (globe).
 * Ported from the Testing revamp artifact (user-test-agent-flow-v80.html,
 * nav V1 · 1.1); stroke converted to currentColor. No Figma node yet.
 */
import type { IconProps } from './types'

export function LocalizationIcon({ size = 20, className, 'aria-label': ariaLabel }: IconProps) {
  return (
    <svg
      width={size}
      height={size}
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth="1.6"
      strokeLinecap="round"
      strokeLinejoin="round"
      xmlns="http://www.w3.org/2000/svg"
      className={className}
      aria-label={ariaLabel}
      aria-hidden={ariaLabel ? undefined : true}
    >
      <circle cx="12" cy="12" r="9" />
      <path d="M3 12h18M12 3c3 3 3 15 0 18M12 3c-3 3-3 15 0 18" />
    </svg>
  )
}
